import { addStyleSheet, element } from "../../framework.js";
addStyleSheet(import.meta.url);
export function ButtonControl(inner, onClick) {
    const button = element("button", { className: "buttoncontrol" }, inner);
    button.addEventListener("click", onClick);
    return button;
}
//# sourceMappingURL=ButtonControl.js.map
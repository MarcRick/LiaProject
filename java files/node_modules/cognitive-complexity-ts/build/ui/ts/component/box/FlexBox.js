import { element, addStyleSheet } from "../../framework.js";
addStyleSheet(import.meta.url);
export class FlexBox {
    constructor() {
        this.dom = element("div", { className: "flexbox" });
    }
    rerender(childNodes) {
        this.dom.innerHTML = "";
        this.dom.append(...childNodes);
    }
}
//# sourceMappingURL=FlexBox.js.map